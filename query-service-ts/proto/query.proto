syntax = "proto3";
package query;

service Query {
     rpc Query(Range) returns (Values);
}

message Range {
    string start = 1;
    string end = 2;
}

message Values {
    repeated string values = 1;
}